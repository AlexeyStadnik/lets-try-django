version: "3.7"
services:

  postgres-db:
    image: postgres:11.5-alpine
    environment:
      - POSTGRES_USER=django
      - POSTGRES_PASSWORD=django
      - POSTGRES_DB=django
    ports:
      - "5432:5432"

